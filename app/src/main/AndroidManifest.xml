<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tahlia.annotation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".retention_runtime.type.TypeActivity"></activity>
        <activity android:name=".retention_source.apt.AptActivity" />
        <activity android:name=".retention_source.apt.BindAptActivity" />
        <activity android:name=".retention_source.check.CheckEntranceActivity" />
        <activity android:name=".retention_runtime.bind.BindActivity" />
        <activity android:name=".retention_runtime.intent.IntentActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>